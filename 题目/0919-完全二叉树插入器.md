---
leetcode: 919
title: 完全二叉树插入器
url: https://leetcode.cn/problems/complete-binary-tree-inserter/
difficulty: medium
tags:
  - bfs
covers:
  - bfs-dfs
sources:
  - labuladong/BFS
date_split: 2026-05-05
solved: true
---

# 919. 完全二叉树插入器

[LeetCode 链接](https://leetcode.cn/problems/complete-binary-tree-inserter/)

## 来源：labuladong / BFS

维护好队列的底部
            if cur.right is None or cur.left is None:
                # 找到完全二叉树底部可以进行插入的节点
                self.q.put(cur)
看代码学习怎么维护
```python
class CBTInserter:

    def __init__(self, root: TreeNode):
        self.root = root
        self.candidate = deque()

        q = deque([root])
        while q:
            node = q.popleft()
            if node.left:
                q.append(node.left)
            if node.right:
                q.append(node.right)
            if not (node.left and node.right):
                self.candidate.append(node)

    def insert(self, val: int) -> int:
        candidate_ = self.candidate

        child = TreeNode(val)
        node = candidate_[0]
        ret = node.val
        
        if not node.left:
            node.left = child
        else:
            node.right = child
            candidate_.popleft()
        
        candidate_.append(child)
        return ret

    def get_root(self) -> TreeNode:
        return self.root
```
