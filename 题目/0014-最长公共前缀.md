---
leetcode: 14
title: 最长公共前缀
url: https://leetcode.cn/problems/longest-common-prefix/
difficulty: easy
tags: []
covers: []
sources:
  - 题单/Grind75
  - 题单/Leetcode150
date_split: 2026-05-05
solved: true
---

# 14. 最长公共前缀

[LeetCode 链接](https://leetcode.cn/problems/longest-common-prefix/)

## 来源：题单 / Grind75

简单题，简单做就好了，要意识到枚举第一个s0的数据
```python
class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        s0 = strs[0]
        for j, c in enumerate(s0):  # 从左到右
            for s in strs:  # 从上到下
                if j == len(s) or s[j] != c:  # 这一列有字母缺失或者不同
                    return s0[:j]  # 0 到 j-1 是公共前缀
        return s0

```

## 来源：题单 / Leetcode150

直接把单词看成一行
```python
class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        s0 = strs[0]
        for j, c in enumerate(s0):  # 从左到右
            for s in strs:  # 从上到下
                if j == len(s) or s[j] != c:  # 这一列有字母缺失或者不同
                    return s0[:j]  # 0 到 j-1 是公共前缀
        return s0
```
