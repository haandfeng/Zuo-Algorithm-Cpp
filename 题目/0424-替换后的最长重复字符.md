---
leetcode: 424
title: 替换后的最长重复字符
url: https://leetcode.cn/problems/longest-repeating-character-replacement/
difficulty: 
tags: []
covers: []
sources:
  - 题单/Blind75
date_split: 2026-05-05
solved: true
---

# 424. 替换后的最长重复字符

[LeetCode 链接](https://leetcode.cn/problems/longest-repeating-character-replacement/)

## 来源：题单 / Blind75

仅仅是替换或者统计个数，不一定要考虑使用dp，能滑动窗口就滑动窗口解决

```python
class Solution
    def characterReplacement(self, s: str, k: int) -> int:
        num = [0] * 26
        n = len(s)
        maxn = left = right = 0

        while right < n:
            num[ord(s[right]) - ord("A")] += 1
            maxn = max(maxn, num[ord(s[right]) - ord("A")])
            if right - left + 1 - maxn > k:
                num[ord(s[left]) - ord("A")] -= 1
                left += 1
            right += 1
        
        return right - left
```
