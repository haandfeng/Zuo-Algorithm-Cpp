---
leetcode: 417
title: 太平洋大西洋水流问题
url: https://leetcode.cn/problems/pacific-atlantic-water-flow/
difficulty: medium
tags:
  - graph
covers:
  - bfs-dfs
  - graph
sources:
  - 灵茶山艾府和代码随想录/图
  - 题单/Blind75
date_split: 2026-05-05
solved: true
---

# 417. 太平洋大西洋水流问题

[LeetCode 链接](https://leetcode.cn/problems/pacific-atlantic-water-flow/)

## 来源：灵茶山艾府和代码随想录 / 图

[卡码网：103. 水流问题](https://kamacoder.com/problempage.php?pid=1175)

注意这里visited的定义，不是看了是visted，而是，进入了dfs，即是合法路径，才是visited
```python
class Solution:
    def pacificAtlantic(self, heights: List[List[int]]) -> List[List[int]]:
        if not heights or not heights[0]:
            return []
        m, n = len(heights), len(heights[0])

        pac = [[False] * n for _ in range(m)]
        atl = [[False] * n for _ in range(m)]
        dirs = [(1, 0), (-1, 0), (0, 1), (0, -1)]

        def dfs(r, c, vis):
            if vis[r][c]:
                return        # 已经访问过，直接退出
            vis[r][c] = True
            for dr, dc in dirs:
                nr, nc = r + dr, c + dc
                if 0 <= nr < m and 0 <= nc < n \
                   and not vis[nr][nc] \
                   and heights[nr][nc] >= heights[r][c]:
                    dfs(nr, nc, vis)

        # 太平洋：上边 + 左边
        for i in range(m):
            dfs(i, 0, pac)
        for j in range(n):
            dfs(0, j, pac)

        # 大西洋：下边 + 右边
        for i in range(m):
            dfs(i, n - 1, atl)
        for j in range(n):
            dfs(m - 1, j, atl)

        res = []
        for i in range(m):
            for j in range(n):
                if pac[i][j] and atl[i][j]:
                    res.append([i, j])
        return res
```

## 来源：题单 / Blind75

```python
class Solution:
    def pacificAtlantic(self, heights: List[List[int]]) -> List[List[int]]:
        if not heights or not heights[0]:
            return []
        m, n = len(heights), len(heights[0])

        pac = [[False] * n for _ in range(m)]
        atl = [[False] * n for _ in range(m)]
        dirs = [(1, 0), (-1, 0), (0, 1), (0, -1)]

        def dfs(r, c, vis):
            if vis[r][c]:
                return        # 已经访问过，直接退出
            vis[r][c] = True
            for dr, dc in dirs:
                nr, nc = r + dr, c + dc
                if 0 <= nr < m and 0 <= nc < n \
                   and not vis[nr][nc] \
                   and heights[nr][nc] >= heights[r][c]:
                    dfs(nr, nc, vis)

        # 太平洋：上边 + 左边
        for i in range(m):
            dfs(i, 0, pac)
        for j in range(n):
            dfs(0, j, pac)

        # 大西洋：下边 + 右边
        for i in range(m):
            dfs(i, n - 1, atl)
        for j in range(n):
            dfs(m - 1, j, atl)

        res = []
        for i in range(m):
            for j in range(n):
                if pac[i][j] and atl[i][j]:
                    res.append([i, j])
        return res

```
