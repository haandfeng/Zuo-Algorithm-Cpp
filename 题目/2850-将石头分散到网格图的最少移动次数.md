---
leetcode: 2850
title: 将石头分散到网格图的最少移动次数
url: https://leetcode.cn/problems/minimum-moves-to-spread-stones-over-grid/
difficulty: 
tags:
  - backtracking
covers:
  - backtracking
sources:
  - 灵茶山艾府和代码随想录/回溯
date_split: 2026-05-05
solved: true
---

# 2850. 将石头分散到网格图的最少移动次数

[LeetCode 链接](https://leetcode.cn/problems/minimum-moves-to-spread-stones-over-grid/)

## 来源：灵茶山艾府和代码随想录 / 回溯

由于所有移走的石子个数等于所有移入的石子个数（即 0 的个数），我们可以把移走的石子的坐标记录到列表 from 中（可能有重复的坐标），移入的石子的坐标记录到列表 to 中。这两个列表的长度是一样的。

枚举 from 的所有排列，与 to 匹配，即累加从 from[i] 到 to[i] 的曼哈顿距离。

所有距离之和的最小值就是答案。

这部分代码使用了 **全排列** 来计算从 from 到 to 的所有可能的匹配方式。next_permutation 会在每次迭代时生成 from 数组的下一个排列。通过计算每个排列下的总移动次数，并更新 ans 为最小的值。
```c++
class Solution {
public:
    int minimumMoves(vector<vector<int>>& grid) {
        vector<pair<int, int>> from, to;
        for (int i = 0; i < grid.size(); i++) {
            for (int j = 0; j < grid[i].size(); j++) {
                if (grid[i][j]) {
                    for (int k = 1; k < grid[i][j]; k++) {
                        from.emplace_back(i, j);
                    }
                } else {
                    to.emplace_back(i, j);
                }
            }
        }

        int ans = INT_MAX;
        do {
            int total = 0;
            for (int i = 0; i < from.size(); i++) {
                total += abs(from[i].first - to[i].first) + abs(from[i].second - to[i].second);
            }
            ans = min(ans, total);
        } while (next_permutation(from.begin(), from.end()));
        return ans;
    }
};
```


# 左链接
[[排列组合]]
[[排列组合-隔板法]]
[[字符串的子串、子序列 & 全排列]]
[[排列序列]]
