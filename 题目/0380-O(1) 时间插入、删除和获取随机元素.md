---
leetcode: 380
title: O(1) 时间插入、删除和获取随机元素
url: https://leetcode.cn/problems/insert-delete-getrandom-o1/
difficulty: medium
tags:
  - design
covers: []
sources:
  - 灵茶山艾府和代码随想录/数据结构
  - 题单/Leetcode150
date_split: 2026-05-05
solved: true
---

# 380. O(1) 时间插入、删除和获取随机元素

[LeetCode 链接](https://leetcode.cn/problems/insert-delete-getrandom-o1/)

## 来源：灵茶山艾府和代码随想录 / 数据结构

看题解，用list来确保获取随机数据的时间是O(1)，用map来保证insert和remove的时间是O(1)
```python
class RandomizedSet:
    def __init__(self):
        self.nums = []
        self.indices = {}

    def insert(self, val: int) -> bool:
        if val in self.indices:
            return False
        self.indices[val] = len(self.nums)
        self.nums.append(val)
        return True

    def remove(self, val: int) -> bool:
        if val not in self.indices:
            return False
        id = self.indices[val]
        self.nums[id] = self.nums[-1]
        self.indices[self.nums[id]] = id
        self.nums.pop()
        del self.indices[val]
        return True

    def getRandom(self) -> int:
        return choice(self.nums)
```

## 来源：题单 / Leetcode150


